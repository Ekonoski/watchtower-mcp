# mcp 2.0.0 (released ~2026-07-29) removed mcp.server.fastmcp — every fresh
# build after the release failed at import while the old image kept serving.
# Pin below 2 until the FastMCP import is migrated deliberately.
mcp[cli]>=1.29,<2
uvicorn[standard]
python-dotenv[cli]

# Core data + live sources for full GMMSS (reversal + momentum + breakdown + regime + research)
pandas
numpy
psycopg2-binary
yfinance
requests
openai

# Polygon (Massive) for live bars, vol surge, RS, SPY regime, options snapshots in bearish sleeve
polygon-api-client

# Scheduler for automated intraday + daily alerts
apscheduler>=3.10.0
pytz

# Error monitoring (inactive unless SENTRY_DSN is set)
sentry-sdk
