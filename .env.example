# Required: claude.ai connector authentication token
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_AUTH_TOKEN=

# Watchtower Supabase DB connection (read-only-capable user preferred)
SUPABASE_DB_HOST=db.ktstmsbfmslvluhqnydb.supabase.co
SUPABASE_DB_PORT=5432
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=
SUPABASE_DB_NAME=postgres

# Optional: server port (Railway sets this automatically)
PORT=8000
